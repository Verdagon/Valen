exported struct Inner share { x int; }
exported struct Outer share { name int; inner Inner; }

extern func testGetters(o Outer) int;

exported func main() int {
  return testGetters(Outer(10, Inner(32)));
}
